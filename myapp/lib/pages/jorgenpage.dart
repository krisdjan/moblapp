import 'package:flutter/material.dart';

class JorgenPage extends StatelessWidget {
  const JorgenPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Jörgeni leht"),)
    );
  }
}