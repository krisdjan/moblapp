import 'package:flutter/material.dart';



class KristjanPage extends StatelessWidget {
  const KristjanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Kristjani leht"),),
    );
  }
}